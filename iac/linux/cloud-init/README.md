# Cloud-init interface

I template `autoinstall/` sono l'input iniziale per le rispettive ISO. Un
renderer futuro deve sostituire solo placeholder approvati e produrre user-data
temporanei. Non aggiungere password, chiavi o configurazioni WireGuard in Git.
