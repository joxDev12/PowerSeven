# common_linux

Scope: base packages, time, locale, SSH policy and safe facts on Ubuntu.
Inputs: `linux_packages_base`, bootstrap user. Requires: none. Must not update
packages implicitly; outputs a predictable Linux base.
