# windows_firewall

Scope: declared Windows Firewall rules for underlay/overlay services. Inputs:
expected ports and access policy. Requires: `ad_ds`, `dns`. Default deny beyond
the declared surface.
