# forgejo

Scope: fresh Forgejo container/config, PostgreSQL connection and LDAP
settings. Inputs: `git.lab.test`, empty volume and secret refs. Requires:
`docker`, `postgresql`, `sssd`. No repository migration.
