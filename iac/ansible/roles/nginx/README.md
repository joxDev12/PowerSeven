# nginx

Scope: host Nginx reverse proxy, virtual hosts, TLS bindings and
firewall-facing ports. Inputs: DNS names, upstream map and certificate refs.
Requires: `common_linux`, `wireguard_linux`. Never stores private keys in Git.
