# nginx

Scope: reverse proxy, virtual hosts, TLS bindings and firewall-facing ports.
Inputs: DNS names, upstream map and certificate refs. Requires: `docker`,
`wireguard`. Never stores private keys in Git.
