# adguard

Scope: fresh AdGuard Home container/config and upstream resolver. Inputs:
underlay bind `192.168.214.14`, image tag and config refs. Requires: `docker`.
Becomes DC02 final forwarder only after health validation.
