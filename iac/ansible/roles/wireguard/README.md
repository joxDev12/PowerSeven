# wireguard

Scope: generate/apply new lab tunnel configuration. Inputs: overlay peers and
external key refs. Requires: `common_linux`. Initial mode is VM peers only;
host client `.101` is a final WG-03 cutover operation.
