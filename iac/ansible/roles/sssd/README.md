# sssd

Scope: LAB.TEST membership, Kerberos/LDAP resolver and access policy on VPS12.
Inputs: underlay DC02 DNS `192.168.214.13`, domain refs and join secret.
Requires: `common_linux`. AD DS/DNS/LDAP are cross-host prerequisites, not
same-host role dependencies.
