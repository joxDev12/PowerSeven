# nextcloud

Scope: fresh Nextcloud container/config, PostgreSQL connection and LDAP
settings. Inputs: `cloud.lab.test`, empty volume and secret refs. Requires:
`docker`, `postgresql`, `sssd`. No old data migration.
