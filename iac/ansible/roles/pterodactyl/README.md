# pterodactyl

Scope: fresh Panel schema/config and queue worker contract. Inputs: MariaDB,
Panel app key ref and `panel.lab.test`. Requires: `docker`, `mariadb`.
Panel is provisioned before Wings runtime configuration.
