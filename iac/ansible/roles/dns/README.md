# dns

Scope: AD-integrated DNS zones, records and forwarder transition. Inputs:
`iac/inventory/dns.yml`; initial forwarder `192.168.214.2`, final AdGuard
`192.168.214.14` on underlay. Requires: `ad_ds`.
